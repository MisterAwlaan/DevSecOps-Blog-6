services:
  web:
    build: .
    container_name: miamstagram_app
    ports:
      - "5000:5000"
    environment:
      - FLASK_DEBUG=1
      - PYTHONUNBUFFERED=1
    # On laisse le volume pour le développement local (modifications en direct)
    volumes:
      - .:/app